#include <stdio.h>

int takeBit(int a, int i);

int main() {

    //тук задавам стойност на двата параметъра
    int a = 15;
    int i = 3;

    printf("%d\n", takeBit(a,i));

    return 0;
}

int takeBit(int a, int i) {

    int h = 1;
    a = a>>i;
    a = a & h;

    return a;
}
